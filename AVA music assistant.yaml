blueprint:
  name: AVA voice for Music Assistant
  author: Lonebaggie (With Unconfigured Remote Speaker Guard)
  homeassistant:
    min_version: 2024.6.0
  description: >
    ### 🗣 AVA Voice Control for Music Assistant
    
    Control music assistant via AVA voice assistants. This blueprint relies on the music assistant
    media players having the same name as the AVA device (so media_player.xxx matches device xxx). 
    Includes remote speaker routing with fallback error checks if the remote entity or input is missing.
    
    <br>
    
    <details>
      <summary><b>ℹ️ Click to view Voice Command Syntax Guide</b></summary>
      <br>
      When adjusting the phrases below, use these wildcards to fine-tune your commands:
      
      * <b>[Square Brackets]</b> = Optional text. The sentence triggers with or without these words.
        * <i>Example:</i> <code>[the]</code> matches both "play album" and "play the album".
        
      * <b>(Parentheses | with | pipes)</b> = Alternation (OR). Matches any single phrase listed.
        * <i>Example:</i> <code>(play|listen to)</code> matches "play..." or "listen to...".
        
      * <b>{Curly Braces}</b> = Wildcard slots. These extract the text to pass to Music Assistant.
        * <i>Warning:</i> Do not change the text inside these braces (<code>{artist}</code>, <code>{album}</code>, <code>{track}</code>, <code>{playlist}</code>, <code>{radio}</code>).
        
      * <b>Nested Brackets</b> = Extra layers of options. <code>[by {artist}]</code> means the artist name is completely optional.
    </details>
  domain: automation
  input:
    default_speaker_options:
      name: Speaker Settings
      icon: mdi:speaker
      collapsed: false
      input:
        default_speaker:
          name: Default Speaker
          description: The speaker to use if voice satellite has no media player.
          selector:
            entity:
              filter:
                - domain: media_player
        remote_speaker:
          name: Remote Speaker
          description: Select the remote media player to target when you specifically say "on remote speaker". Leave blank if unused.
          default: ""
          selector:
            entity:
              filter:
                - domain: media_player

    # --- Collapsible Artist Section ---
    artist_options:
      name: Artist Voice Commands
      icon: mdi:account-music
      description: Custom phrases for managing artist selection and playback.
      collapsed: true
      input:
        artist_sentence_1:
          name: Play Artist Phrase
          description: >
            Phrase used to play an artist's music.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{artist}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(play|listen to|put on|stream) [the ](artist|band|group|musician) {artist} [on remote speaker]"
          selector:
            text:
        artist_sentence_2:
          name: Shuffle Artist Phrase
          description: >
            Phrase used to shuffle an artist's music.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{artist}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(shuffle|randomise) [the ](artist|band|group|musician) {artist} [on remote speaker]"
          selector:
            text:

    # --- Collapsible Track / Song Section ---
    track_options:
      name: Song Voice Commands
      icon: mdi:music-note
      description: Custom phrases for playing specific songs or tracks.
      collapsed: true
      input:
        track_sentence_1:
          name: Play Song Phrase
          description: >
            Phrase used to play a specific song or track.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{track}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(play|listen to|put on|stream) [the ](song|track|tune) {track}[ [by ][the ](artist|band|group|musician) {artist}] [on remote speaker]"
          selector:
            text:
        track_sentence_2:
          name: Shuffle Song Phrase
          description: >
            Phrase used to play a song with shuffling/radio fallback enabled.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{track}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(shuffle|randomise) [the ](song|track|tune) {track}[ [by ][the ](artist|band|group|musician) {artist}] [on remote speaker]"
          selector:
            text:

    # --- Collapsible Playlist Section ---
    playlist_options:
      name: Playlist Voice Commands
      icon: mdi:playlist-music
      description: Custom phrases for managing playlist selection and playback.
      collapsed: true
      input:
        playlist_sentence_1:
          name: Play Playlist Phrase
          description: >
            Phrase used to play a specific playlist.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{playlist}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(play|listen to|put on|stream) [the ](playlist) {playlist} [on remote speaker]"
          selector:
            text:
        playlist_sentence_2:
          name: Shuffle Playlist Phrase
          description: >
            Phrase used to shuffle a specific playlist.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{playlist}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(shuffle|randomise) [the ](playlist) {playlist} [on remote speaker]"
          selector:
            text:

    # --- Collapsible Album Section ---
    album_options:
      name: Album Voice Commands
      icon: mdi:album
      description: Custom phrases for managing album selection and playback.
      collapsed: true
      input:
        album_sentence_1:
          name: Play Album Phrase
          description: >
            Phrase used to play an album.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{album}</b></code> <code><b>{artist}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(play|listen to|put on|stream) [the ](album|ep|record|compilation) {album}[ [by ][the ](artist|band|group) {artist}] [on remote speaker]"
          selector:
            text:
        album_sentence_2:
          name: Shuffle Album Phrase
          description: >
            Phrase used to shuffle an album.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{album}</b></code> <code><b>{artist}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(shuffle|randomise)[the ](album|ep|record|compilation) {album}[ [by ][the ](artist|band|group) {artist}] [on remote speaker]"
          selector:
            text:

    # --- Collapsible Radio Section ---
    radio_options:
      name: Radio Voice Commands
      icon: mdi:radio
      description: Custom phrases for tuning into radio stations.
      collapsed: true
      input:
        radio_sentence:
          name: Play Radio Phrase
          description: >
            Phrase used to tune into a radio station.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Mandatory</b> <code><b>{radio}</b></code> <b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(play|listen to|put on|turn on|stream) [the ](radio|BBC|station|radio station) {radio} [on remote speaker]"
          selector:
            text:

    # --- Collapsible Audio Controls Section ---
    audio_controls:
      name: System Controls (Playback, Volume & Stop)
      icon: mdi:volume-high
      description: Custom phrases for managing volume, tracking skipping, and halting playback.
      collapsed: true
      input:
        next_sentence:
          name: Next Track Phrase
          description: >
            Phrase used to skip to the next track.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(next track|next song|skip|skip track|skip song) [on remote speaker]"
          selector:
            text:
        previous_sentence:
          name: Previous Track Phrase
          description: >
            Phrase used to return to the previous track.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(previous track|previous song|back|go back) [on remote speaker]"
          selector:
            text:
        volume_up_sentence:
          name: Volume Up Phrase
          description: >
            Phrase used to turn up the volume.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(volume up|louder|turn it up|crank it up|raise the volume) [on remote speaker]"
          selector:
            text:
        volume_down_sentence:
          name: Volume Down Phrase
          description: >
            Phrase used to lower the volume.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(volume down|quieter|turn it down|lower the volume) [on remote speaker]"
          selector:
            text:
        stop_sentence:
          name: Stop Music Phrase
          description: >
            Phrase used to halt playback.
            <details><summary><b>💡 Quick Syntax Summary</b></summary><b>Words ignored </b> <code><b>[ ]</b></code> <b>Match word alternatives</b> <code><b>( | )</b></code></details>
          default: "(stop|cancel|pause|hush|shut up) [the ][music|audio|radio|playback] [on remote speaker]"
          selector:
            text:

mode: single

trigger:
  - trigger: conversation
    command:
      - !input artist_sentence_1
      - !input artist_sentence_2
    id: artist
  - trigger: conversation
    command:
      - !input track_sentence_1
      - !input track_sentence_2
    id: track
  - trigger: conversation
    command:
      - !input playlist_sentence_1
      - !input playlist_sentence_2
    id: playlist
  - trigger: conversation
    command:
      - !input album_sentence_1
      - !input album_sentence_2
    id: album
  - trigger: conversation
    command:
      - !input radio_sentence
    id: radio
  - trigger: conversation
    command:
      - !input next_sentence
    id: media_next
  - trigger: conversation
    command:
      - !input previous_sentence
    id: media_previous
  - trigger: conversation
    command:
      - !input volume_up_sentence
    id: volume_up
  - trigger: conversation
    command:
      - !input volume_down_sentence
    id: volume_down
  - trigger: conversation
    command:
      - !input stop_sentence
    id: media_stop

condition: []

action:
  - variables:
      sat: "{{trigger.satellite_id.split('.') if trigger.satellite_id is defined and trigger.satellite_id is not none and '.' in trigger.satellite_id else ['', '']}}"
      default: !input default_speaker
      remote: !input remote_speaker
      spoken_sentence: "{{ trigger.sentence | default('', true) | lower }}"
      
      # Determine base local/satellite target
      base_speaker: >
        {% if sat | length > 1 and sat[1] != "" %}
          media_player.{{sat[1]}}
        {% else %}
          {{ default }}
        {% endif %}

      # Clean replacement logic: If "on remote speaker" is in the text, use remote. Otherwise base local.
      speaker: >
        {% if 'on remote speaker' in spoken_sentence and remote is defined and remote | length > 0 %}
          {{ remote }}
        {% else %}
          {{ base_speaker }}
        {% endif %}

      # Guard check 1: User asked for remote speaker, but none was defined in the blueprint
      no_remote_configured: >
        {% set explicit_remote = 'on remote speaker' in spoken_sentence %}
        {{ explicit_remote and (remote is not defined or remote | length == 0) }}

      # Guard check 2: Remote speaker was defined, but it's offline or dead
      remote_unavailable: >
        {% set explicit_remote = 'on remote speaker' in spoken_sentence %}
        {{ explicit_remote and remote is defined and remote | length > 0 and states(remote) in ['unavailable', 'unknown', 'off'] }}

  - choose:
      # Branch 0a: Guard clause for unconfigured remote speaker input
      - conditions:
          - condition: template
            value_template: "{{ no_remote_configured }}"
        sequence:
          - set_conversation_response: "No remote speaker setup."
          - stop: "No remote speaker configured"

      # Branch 0b: Guard clause for offline/unavailable remote speaker entity
      - conditions:
          - condition: template
            value_template: "{{ remote_unavailable }}"
        sequence:
          - set_conversation_response: "Remote speaker not ready."
          - stop: "Remote speaker unavailable"

  - variables:
      radio: "{{ trigger.slots.radio | default('') }}"
      artist: "{{ trigger.slots.artist | default('') }}"
      album: "{{ trigger.slots.album | default('') }}"
      playlist: "{{ trigger.slots.playlist | default('') }}"
      track: "{{ trigger.slots.track | default('') }}"

      media_type: "{{ trigger.id.split('_') | first }}"
      
      # Transport action assignment
      action: >-
        {% if trigger.id == 'media_stop' %}
          media_player.media_stop
        {% elif trigger.id == 'media_next' %}
          media_player.media_next_track
        {% elif trigger.id == 'media_previous' %}
          media_player.media_previous_track
        {% else %}
          media_player.{{ media_type }}
        {% endif %}

      media_id: "{{ radio }} {{ artist }} {{ album }} {{ playlist }} {{ track }}"
      shuffle: >
        {{ 'shuffle' in spoken_sentence or 'random' in spoken_sentence }}

      # Math section for calculated volume stepping
      current_volume: "{{ state_attr(speaker, 'volume_level') | float(0.3) }}"
      target_volume: >-
        {% if trigger.id == 'volume_up' %}
          {{ ([current_volume + 0.05, 1.0] | min) | round(2) }}
        {% elif trigger.id == 'volume_down' %}
          {{ ([current_volume - 0.05, 0.0] | max) | round(2) }}
        {% else %}
          {{ current_volume }}
        {% endif %}

  - action: media_player.shuffle_set
    target:
      entity_id: "{{ speaker }}"
    data:
      shuffle: false

  - choose:
      # Branch 1: Unified calculated volume tracking
      - conditions:
          - condition: template
            value_template: "{{ trigger.id in ['volume_up', 'volume_down'] }}"
        sequence:
          - action: media_player.volume_set
            target:
              entity_id: "{{ speaker }}"
            data:
              volume_level: "{{ target_volume }}"

      # Branch 2: Standard Transport Controls (Stop, Skip, Prev)
      - conditions:
          - condition: template
            value_template: "{{ trigger.id in ['media_stop', 'media_next', 'media_previous'] }}"
        sequence:
          - action: "{{ action }}"
            target:
              entity_id: "{{ speaker }}"
            data: {}

      # Branch 3: Music Assistant query routing
      - conditions:
          - condition: template
            value_template: "{{ trigger.id in ['artist', 'track', 'album', 'radio', 'playlist'] }}"
        sequence:
          - action: media_player.shuffle_set
            target:
              entity_id: "{{ speaker }}"
            data:
              shuffle: "{{ shuffle }}"
          - action: music_assistant.play_media
            target:
              entity_id: "{{ speaker }}"
            data:
              media_id: "{{ media_id | trim }}"
              artist: "{{ artist }}"
              album: "{{ album }}"
              enqueue: play
              media_type: "{{ media_type }}"